name: ansible-lint
on: [push, pull_request]

jobs:
  build:
    name: Ansible Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Go linting
        uses: ansible/ansible-lint@main